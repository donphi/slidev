services:
  slidev:
    build: .
    ports:
      - "3030:3030"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    # Enable polling for file changes if on systems where events don't propagate
    environment:
      - CHOKIDAR_USEPOLLING=true

